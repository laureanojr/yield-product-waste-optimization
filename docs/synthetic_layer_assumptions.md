# Synthetic Manufacturing Layer — Declared Assumptions

This document states every fabricated assumption behind the synthetic layer. It exists so that a reader can tell, without reading code, exactly which numbers came from the real dataset and which were invented.

**The single most important statement in this repository:**

> The synthetic layer produces no findings, by construction. I wrote the data, so any pattern in it is my own assumption reflected back at me. This layer demonstrates that a production data model can be built and that OEE can be derived correctly from raw events. The real analysis is in the POS layer.

---

## The two layers

The repository is one project with two layers, distinguished by provenance.

| | Real layer | Synthetic layer |
|---|---|---|
| Source | French bakery POS, Jan 2021 – Sep 2022 | Generated by a script in this repo |
| Content | 232,679 cleaned sales lines, 147 products | Production batches, downtime, quality inspections |
| Purpose | Analysis. Real findings. | Method demonstration. No findings. |

The only real value carried into the synthetic layer is `dim_product.v1_total_units` — the **reference outlet demand**. Real data informs the *shape* of the simulation, never its contents.

---

## The assumed plant

There is no equipment data in the source dataset. The plant below is declared, not observed.

> A central production bakery supplying **ten retail outlets**. Three production resources with fixed product routing: a deck oven line (bread), a rack oven line (viennoiserie and patisserie), and a manual assembly bench (sandwich). Manual loading and unloading throughout.

### Why the plant supplies ten outlets

The real POS series describes one retail shop: roughly 540 units a day across all 27 modelled products. A single shop does not run an OEE programme — it has a baker who knows what to bake. Anchoring an industrial metric to shop-scale demand produces ovens that stand idle and an OEE that describes emptiness rather than performance.

So the generator multiplies reference outlet demand by a declared constant, `OUTLET_EQUIVALENT_FACTOR = 10`, **before production planning** — not after generation. The demand *mix* and *seasonality* still come from the real dataset; only the *scale* is fabricated.

Ten is a judgement, not a calculation. It produces a plant with a few thousand units a day and several hours of daily oven time, which is a plant where OEE is a metric someone would plausibly track. That is the entire justification and it is not dressed up as more.

### Operating calendar

The plant runs **Monday to Saturday**, closed Sundays, on its own schedule. It does not inherit the retail shop's Wednesday closure — a production bakery supplying ten outlets has no reason to share one shop's opening hours.

---

## Equipment basis

Six fabricated numbers. Every one of the 27 production rates derives from them plus a bake time.

| Parameter | Assumption |
|---|---|
| Deck oven — decks | 3 |
| Deck oven — baking surface | ~120 × 160 cm per deck |
| Deck oven — baguettes per deck | 30 (laid width-wise, ~5 cm pitch: piece plus a 2–3 cm airflow gap) → 90 per full load |
| Deck oven — handling allowance per cycle | 6 minutes (2 min per deck, product staged at the bench, peel loaded before the door opens) |
| Rack oven — trays per rack | 18 (600 × 400 mm, ~85 mm spacing) |
| Rack oven — croissants per tray | 16 (4 × 4, 70 g standard retail size) → 288 per full rack |
| Rack oven — handling allowance per cycle | 2 minutes (trolley swap, door-open to door-closed) |

The airflow gap is not a rounding allowance — it is the constraint. Pack baguettes tighter than about 2–3 cm apart and the side crust never sets, so the bake comes out uneven. The same reasoning fixes the tray layouts: a 70 g croissant goes 4 × 4 on a 600 × 400 tray, not 5 × 5, because the pieces need room for oven spring without their edges touching.

The handling allowance is why cycle time is not simply bake time. The deck oven is slower because three chambers are cleared and reloaded by hand. The rack oven is faster because a trolley rolls out and another rolls in. Both figures are working targets, not slow-day observations: a six-minute rack swap would bleed steam and thermal mass and leave a trolley gradient — the top trays coming out different from the bottom.

### The load unit is the tray (or the deck), not the whole oven

A rack is a **shared resource**. No bakery bakes eighteen trays of nothing but éclairs. A batch occupies the trays it needs and consumes oven time in proportion:

```
run_time = (trays_used / 18) × cycle_minutes
```

Same on the deck oven, with decks. Allocating a fraction of an oven cycle is a mild fiction — you cannot run an eighteenth of a rack — but it is the standard capacity-share allocation an MES uses when a resource is shared across concurrent work orders. Without it, `Performance` becomes a measure of how I chose to batch rather than how the process ran.

### Rate definition

```
ideal_units_per_hr = units_per_full_load × (60 / (bake_minutes + handling_minutes))
```

Reference rates: **traditional baguette 180.00/hr** (90 per load, 30-minute cycle) and **croissant 864.00/hr** (288 per rack, 20-minute cycle).

**`ideal_units_per_hr` means nominal bake-cell capacity**: theoretical maximum finished units per hour *of run time*, at full load, no changeover, no downtime.

**Finishing and labour stations are not modelled.** Hand-finished products — éclair, tartelette, the almond products — therefore show lower Performance against an oven-based standard. That is a stated simplification. Modelling the finishing constraint would require labour standards for seven products that this project cannot defend, in a layer that produces no findings anyway.

`SANDWICH COMPLET` is the only non-oven product: 60 seconds per completed unit, so 60 units/hr.

### Footprint factors

Load sizes derive from each product's footprint relative to the reference product on its machine. This is geometry — a round loaf uses deck width less efficiently than a long thin one — and it is reasoned about openly rather than claimed from plant experience.

Load sizes are whole pieces per deck or per tray, because ovens hold whole pieces.

Deck oven — pieces per deck × 3 decks:

| Class | Per deck | Full load |
|---|---|---|
| Ficelle (thinner than a baguette, tighter pitch) | 39 | 117 |
| Banettine | 36 | 108 |
| Baguette format | 30 | 90 |
| Boule 200 g | 18 | 54 |
| Standard loaf | 15 | 45 |
| Boule 400 g | 12 | 36 |
| Country / specialty loaf | 9 | 27 |

Rack oven — pieces per 600 × 400 tray × 18 trays:

| Class | Layout | Per tray | Full rack |
|---|---|---|---|
| Cookie | 6 × 4 | 24 | 432 |
| Éclair | 5 × 4 | 20 | 360 |
| Croissant (70 g) | 4 × 4 | 16 | 288 |
| Pain au chocolat, raisins, kouign amann, almond products, tartelette | 4 × 3 | 12 | 216 |
| Chausson aux pommes | 4 × 2 | 8 | 144 |

### Bake times

Baguette 24 min · ficelle and banettine 22 · cereal baguette 26 · boule 200 g 28 · standard loaf 32 · boule 400 g 35 · specialty loaf 40 · country and wholemeal loaf 42 · croissant 18 · pain au chocolat 19 · raisins, almond products, tartelette 20 · chausson 22 · kouign amann 24 · éclair shell 29 · cookie 13.

---

## Cost

`unit_cost_eur` is **full standard cost per unit** (materials + conversion), used to value scrap.

Derived as: real median observed selling price × a fabricated category cost ratio — bread 0.30, viennoiserie 0.36, patisserie 0.40, sandwich 0.45.

**Simplification:** all scrap is valued at full standard cost regardless of the stage at which it was rejected. A real plant values scrap at the cost accumulated up to rejection — dough rejected at proofing is cheap, a burnt loaf pulled at final inspection carries the full bake. Modelling that needs a multi-stage process this schema does not have.

**Circularity warning:** the cost ratios are invented. Any statement of the form "category X carries the most scrap cost" is those ratios read back. Cost is a scaling constant, not a source of insight.

---

## The MES pathology (deliberate)

Real production data is systematically wrong in a particular way, and the generator reproduces it on purpose.

**Downtime reason codes get miscoded to the catch-all.** The terminal offers a list of reasons; "Other" is the fastest button; a meaningful share of events that were really changeovers or short stops end up there. The generator writes the *true* reason, then miscodes a declared proportion to `OTHER`.

A data-quality view surfaces the result: `OTHER` is the single largest downtime bucket, which makes the downtime Pareto unusable as a maintenance-prioritisation tool until the reason coding is fixed.

This is a finding about **data quality**, not about bread. It violates no rule — it demonstrates that an analyst knows where operational data goes wrong and checks before trusting it. *Distrust the data first.*

---

## Known limitations

**Fixed routing means machine is collinear with product family.** Bread is always on the old deck oven; viennoiserie always on the newer rack. A downtime pattern on the deck line cannot be distinguished from a downtime pattern on bread. Harmless here — this layer produces no findings — but stated rather than hidden.

**Machine age is the only causal channel from equipment to data.** Install year → base failure rate → downtime events → OEE Availability. There is no fabricated "condition score" doing invisible work.

**No demand seasonality is *discovered*.** The demand tie makes the data plausible; it is never the insight. Injecting summer seasonality and then "finding" summer overproduction would be finding nothing but my own assumptions.

**Planned production time = scheduled batch time**, written explicitly by the generator — not shift length. Availability measures downtime against the plan, which is what it is for.

**These standards are part remembered, part declared, and the difference is marked.** The bake times, load densities and handling allowances come from nine years in industrial food production, including production-oven work — they are the strongest term in every cycle time here, and they are the reason the load densities carry an airflow gap rather than being packed to the theoretical maximum. What is *declared* is the plant itself: no equipment data exists in the source dataset, so the deck count, rack size and machine ages are an assumption about a simulated facility, stated openly, with every rate deriving from it. Experience sets the process values. The plant is an assumption. Both belong in the project; only one is claimed as experience.
